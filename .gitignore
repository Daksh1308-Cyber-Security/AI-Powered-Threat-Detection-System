# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/
venv/
env/

# Secrets — never commit these
.env

# Lab artifacts (VM disks, captures)
VMs/
*.vdi
*.vbox
*.pcap
*.log

# Downloaded benchmark datasets (CICIDS-2017, UNSW-NB15) — large, re-downloadable,
# and licensed for academic use only. Fetch them locally per the README instead
# of committing them to the repo.
lab/datasets/kaggle/

# Large/partial download fragments
*.zip.partial
*.kaggle-partial
*.kaggle

# OS / editor junk
.DS_Store
Thumbs.db
.idea/
.vscode/